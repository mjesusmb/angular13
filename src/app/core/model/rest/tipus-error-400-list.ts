import { TipusError400 } from './tipus-error-400';

/**
 * La llista d'errors en propietats detectats pel servidor.
 */
// tslint:disable-next-line:no-empty-interface
export interface TipusError400List extends Array<TipusError400> {
}
